import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../l10n/app_localizations.dart';
import '../../causes/models/organisation.dart';
import '../models/create_donation.dart';
import '../providers/donation_providers.dart';

class DonationPage extends ConsumerStatefulWidget {
  const DonationPage({super.key, required this.causeId, required this.causeName, required this.organisation, required this.organisations});
  final String causeId;
  final String causeName;
  final Organisation organisation;
  final List<Organisation> organisations;
  @override ConsumerState<DonationPage> createState() => _DonationPageState();
}

class _DonationPageState extends ConsumerState<DonationPage> {
  final _amountController = TextEditingController();
  final Map<String, TextEditingController> _allocationControllers = {};
  final Map<String, double> _previousAllocations = {};
  int? _selectedAmount;
  bool _submitting = false;
  static const _amounts = [100, 500, 1000, 2000];

  @override void initState() { super.initState(); for (final o in widget.organisations) { _allocationControllers[o.id] = TextEditingController(text: '0'); _previousAllocations[o.id] = 0; } }
  @override void dispose() { _amountController.dispose(); for (final c in _allocationControllers.values) { c.dispose(); } super.dispose(); }
  double _number(String value) => double.tryParse(value.trim()) ?? 0;
  double get _totalAmount => _number(_amountController.text);
  double get _allocatedAmount => _allocationControllers.values.fold<double>(0, (sum, c) => sum + _number(c.text));
  double get _remainingAmount => _totalAmount - _allocatedAmount;
  bool get _amountSelected => _totalAmount > 0;
  bool get _allocationComplete => _amountSelected && _remainingAmount.abs() < 0.005;

  void _selectAmount(int amount) { setState(() { _selectedAmount = amount; _amountController.text = amount.toString(); _setInitialAllocation(); }); }
  void _setInitialAllocation() { for (final o in widget.organisations) { final double value = o.id == widget.organisation.id ? _totalAmount : 0.0; _allocationControllers[o.id]!.text = value.toStringAsFixed(0); _previousAllocations[o.id] = value; } }
  void _resetAllocations() { for (final c in _allocationControllers.values) { c.text = '0'; } for (final id in _previousAllocations.keys) { _previousAllocations[id] = 0.0; } }
  void _onTotalChanged(String value) { final parsed = double.tryParse(value.trim()); setState(() { _selectedAmount = parsed != null && _amounts.contains(parsed.toInt()) ? parsed.toInt() : null; if (parsed != null && parsed > 0) { _setInitialAllocation(); } else { _resetAllocations(); } }); }
  void _onAllocationChanged(String id, String value) { final controller = _allocationControllers[id]!; final entered = _number(value); final otherAllocated = _previousAllocations.entries.where((e) => e.key != id).fold<double>(0.0, (sum, e) => sum + e.value); final double maximum = (_totalAmount - otherAllocated).clamp(0.0, _totalAmount).toDouble(); final double adjusted = entered.clamp(0.0, maximum).toDouble(); if (adjusted != entered) { controller.text = adjusted.toStringAsFixed(0); controller.selection = TextSelection.fromPosition(TextPosition(offset: controller.text.length)); } _previousAllocations[id] = adjusted; setState(() {}); }

  Future<void> _submit() async { final l10n = AppLocalizations.of(context)!; final total = _totalAmount; if (total <= 0) { ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(l10n.donationInvalidAmount))); return; } if (!_allocationComplete) { ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Please distribute the complete donation amount before continuing.'))); return; } final allocations = <CreateDonationAllocation>[]; for (final o in widget.organisations) { final amount = _number(_allocationControllers[o.id]!.text); if (amount > 0) allocations.add(CreateDonationAllocation(causeId: widget.causeId, organisationId: o.id, amount: amount.toStringAsFixed(2))); } setState(() => _submitting = true); try { await ref.read(donationRepositoryProvider).createDonation(CreateDonation(amount: total.toStringAsFixed(2), allocations: allocations)); if (!mounted) return; await showDialog<void>(context: context, builder: (context) => AlertDialog(title: Text(l10n.donationCreatedTitle), content: Text(l10n.donationCreatedMessage), actions: [TextButton(onPressed: () => Navigator.pop(context), child: Text(l10n.done))])); if (mounted) Navigator.pop(context); } catch (_) { if (!mounted) return; ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(l10n.donationCreateError))); } finally { if (mounted) setState(() => _submitting = false); } }

  @override Widget build(BuildContext context) { final l10n = AppLocalizations.of(context)!; final theme = Theme.of(context); final remaining = _remainingAmount; return Scaffold(appBar: AppBar(title: Text(l10n.donateTitle)), body: ListView(padding: const EdgeInsets.fromLTRB(20,20,20,32), children: [Container(padding: const EdgeInsets.all(22), decoration: BoxDecoration(gradient: const LinearGradient(begin: Alignment.topLeft,end: Alignment.bottomRight,colors: [Color(0xFF6E1A14),Color(0xFF4C120D)]),borderRadius: BorderRadius.circular(24),border: Border.all(color: const Color(0xFFC89B3C))), child: Column(crossAxisAlignment: CrossAxisAlignment.start,children: [const Text('दान',style: TextStyle(color: Color(0xFFF5A623),fontWeight: FontWeight.w700,fontSize: 14)),const SizedBox(height:8),Text(widget.causeName,style: theme.textTheme.headlineSmall?.copyWith(color: Colors.white,fontSize:25)),const SizedBox(height:6),Text('आपका कुल सहयोग पहले चुनें, फिर उसे संस्थाओं में बाँटें।',style: TextStyle(color: Colors.white.withValues(alpha:0.82),fontSize:14))])),const SizedBox(height:28),Text(l10n.chooseAmount,style: theme.textTheme.titleLarge),const SizedBox(height:14),GridView.builder(shrinkWrap:true,physics:const NeverScrollableScrollPhysics(),itemCount:_amounts.length,gridDelegate:const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount:2,crossAxisSpacing:12,mainAxisSpacing:12,childAspectRatio:2.5),itemBuilder:(context,index){final amount=_amounts[index];final selected=_selectedAmount==amount;return OutlinedButton(key:ValueKey('donation_amount_$amount'),onPressed:()=>_selectAmount(amount),style:OutlinedButton.styleFrom(backgroundColor:selected?const Color(0xFFFCE8C9):Colors.white,foregroundColor:const Color(0xFF6E1A14),side:BorderSide(color:selected?const Color(0xFFC89B3C):const Color(0xFFE8DCC8),width:selected?1.5:1),shape:RoundedRectangleBorder(borderRadius:BorderRadius.circular(14))),child:Text('₹ $amount'));}),const SizedBox(height:22),TextField(key:const ValueKey('donation_amount_input'),controller:_amountController,keyboardType:const TextInputType.numberWithOptions(decimal:true),onChanged:_onTotalChanged,decoration:InputDecoration(labelText:l10n.customAmount,prefixText:'₹ ',filled:true,fillColor:Colors.white,border:OutlineInputBorder(borderRadius:BorderRadius.circular(14)))),if(_amountSelected)...[const SizedBox(height:30),Text('वितरण',style:theme.textTheme.titleLarge),const SizedBox(height:6),Text(remaining.abs()<0.005?'पूरी राशि वितरित हो चुकी है':'₹ ${remaining.toStringAsFixed(0)} शेष राशि वितरित करें',style:theme.textTheme.bodyMedium),const SizedBox(height:14),...widget.organisations.map((o){final controller=_allocationControllers[o.id]!;return Padding(padding:const EdgeInsets.only(bottom:12),child:Card(child:Padding(padding:const EdgeInsets.all(14),child:Row(children:[Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(o.name,style:theme.textTheme.titleMedium),if(o.city!=null)Text('${o.city}, ${o.state??''}'.trim())])),const SizedBox(width:12),SizedBox(width:120,child:TextField(key:ValueKey('donation_allocation_${o.id}'),controller:controller,keyboardType:const TextInputType.numberWithOptions(decimal:true),onChanged:(value)=>_onAllocationChanged(o.id,value),decoration:const InputDecoration(prefixText:'₹ ',isDense:true)))],))));}),const SizedBox(height:10),Container(padding:const EdgeInsets.all(16),decoration:BoxDecoration(color:_allocationComplete?const Color(0xFFE9F5E9):const Color(0xFFFCE8C9),borderRadius:BorderRadius.circular(16)),child:Row(mainAxisAlignment:MainAxisAlignment.spaceBetween,children:[Text('वितरित: ₹ ${_allocatedAmount.toStringAsFixed(0)}'),Text('कुल: ₹ ${_totalAmount.toStringAsFixed(0)}',style:const TextStyle(fontWeight:FontWeight.w700))])),const SizedBox(height:18),FilledButton.icon(key:const ValueKey('donation_submit'),onPressed:_submitting||!_allocationComplete?null:_submit,icon:_submitting?const SizedBox(width:18,height:18,child:CircularProgressIndicator(strokeWidth:2)):const Icon(Icons.favorite_rounded),label:Text(l10n.donateNow)),const SizedBox(height:12),Text(l10n.donationPaymentLater,textAlign:TextAlign.center,style:theme.textTheme.bodySmall)],])); }
}
